# Pione Server

## Cài đặt
```bash
# Cài đặt các thư viện cần thiết
pip install -r requirements.txt
```

## Chạy server
```bash
# Server sẽ chạy ở port 8000
uvicorn main:app --host 0.0.0.0 --port 8000
```

API sẽ có sẵn tại: http://localhost:8000
