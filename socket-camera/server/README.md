# Image Processing Server

## How to Run

To run the server with auto-reload for development, use the following command from within the `server` directory:

```bash
uvicorn main:app --host 0.0.0.0 --port 8001
```

The server will start on `http://localhost:8000`.
